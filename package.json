{
  "name": "chatgpt-image-fanout",
  "version": "0.3.1",
  "private": true,
  "description": "Fan one image concept out into 2–8 independent ChatGPT sessions with separate variance instructions.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "node build.mjs all",
    "build:chrome": "node build.mjs chrome",
    "build:firefox": "node build.mjs firefox",
    "package": "node build.mjs all --zip",
    "test": "node test/extension.test.cjs"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}
